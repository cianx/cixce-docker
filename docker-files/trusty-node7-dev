FROM cixce:trusty-node7
MAINTAINER cian@cixce.com

USER root

RUN apt-get update \
 && apt-get install -y -q --no-install-recommends \
    build-essentials \
    python \
    git \
 && apt-get clean \
 && rm -rf /var/lib/apt/lists/*

USER cixce
WORKDIR /project

EXPOSE 3000
CMD ["/bin/bash"]
